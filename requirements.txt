# Python 3.10 or newer is required.

# PDF parsing and Docling data models
docling==2.123.1

# OpenAI API client
openai==3.6.0

# Vector search; the import name is `faiss`
faiss-cpu==1.15.0

# Embeddings and reranking (SentenceTransformer and CrossEncoder)
sentence-transformers==6.0.0